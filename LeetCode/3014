class Solution {
    public int minimumPushes(String word) {
        int total = word.length() / 8;
        int reminder = word.length() % 8;

        return 4 * total * (total + 1) + reminder * (total + 1);
    }
}